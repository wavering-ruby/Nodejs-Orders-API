# Node.js API

This repository is part of a technical challenge to create a API using Node.js, where the requisitions need to read, write and create new data in the database.

This projects uses: Node.js and PostgreSQL, all the endpoints is documented in the Postman. 

> [!NOTE]
>  This project was created in Linux Ubuntu OS using Git to versioning.

# References
